package com.example.superslidegame.game.entities

data class User(val userName: String, val levelsPassed: Int) {
    //Levels passed to know which levels has passed. (They'll have another background color)
}